{% extends "admin/layout.twig" %}

{% set active = 'events' %}

{% block admin_content %}
  <wa-card class="admin-card">
    <div class="admin-header-row">
      <div>
        <div class="admin-title">Events</div>
        <div class="admin-subtitle">Create and manage event tickets.</div>
      </div>
      <wa-button href="/admin/events/new" variant="primary">New Event</wa-button>
    </div>

    <div class="admin-table-wrap admin-table-wrap--responsive">
      <table class="admin-table admin-table--responsive">
        <thead>
          <tr>
            <th>Name</th>
            <th>Slug</th>
            <th>Status</th>
            <th>Start</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          {% for row in events %}
            <tr>
              <td data-label="Name">{{ row.name }}</td>
              <td data-label="Slug" class="mono">{{ row.slug }}</td>
              <td data-label="Status"><wa-tag size="s">{{ row.status }}</wa-tag></td>
              <td data-label="Start" class="mono">{{ row.start_time ?? '' }}</td>
              <td data-label="Actions"><wa-button href="/admin/events/{{ row.id }}" variant="neutral" size="s">Edit</wa-button></td>
            </tr>
          {% else %}
            <tr>
              <td colspan="5">No events yet.</td>
            </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
  </wa-card>
{% endblock %}
