{% extends "admin/layout.twig" %}

{% set active = 'events' %}

{% block admin_content %}
  <wa-card class="admin-card">
    <div class="admin-header-row">
      <div>
        <div class="admin-title">Variations</div>
        <div class="admin-subtitle">{{ event.name }}</div>
      </div>
      <div class="admin-actions-row">
        <wa-button href="/admin/events/{{ event.id }}" variant="neutral">Back</wa-button>
        <wa-button href="/admin/events/{{ event.id }}/variations/new" variant="primary">New Variation</wa-button>
      </div>
    </div>

    <div class="admin-table-wrap admin-table-wrap--responsive">
      <table class="admin-table admin-table--responsive">
        <thead>
          <tr>
            <th>Name</th>
            <th>Price</th>
            <th>Stock Limit</th>
            <th>Sort</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          {% for row in variations %}
            <tr>
              <td data-label="Name">{{ row.name }}</td>
              <td data-label="Price" class="mono">{{ row.price_cents|format_money }}</td>
              <td data-label="Stock Limit" class="mono">{{ row.stock_limit ?? '' }}</td>
              <td data-label="Sort" class="mono">{{ row.sort_order }}</td>
              <td data-label="Actions"><wa-button href="/admin/events/{{ event.id }}/variations/{{ row.id }}" variant="neutral" size="s">Edit</wa-button></td>
            </tr>
          {% else %}
            <tr><td colspan="5">No variations yet.</td></tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
  </wa-card>
{% endblock %}
