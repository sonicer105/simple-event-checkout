{% extends "admin/layout.twig" %}

{% set active = 'events' %}

{% block admin_content %}
  <wa-card class="admin-card">
    <div class="admin-header-row">
      <div>
        <div class="admin-title">Variations</div>
        <div class="admin-subtitle">{{ event.name }}</div>
      </div>
      <div class="admin-actions-row">
        <wa-button href="/admin/events/{{ event.id }}" variant="neutral">Back</wa-button>
        <wa-button href="/admin/events/{{ event.id }}/variations/new" variant="primary">New Variation</wa-button>
      </div>
    </div>

    <div class="admin-table-wrap">
      <table class="admin-table">
        <thead>
          <tr>
            <th>Name</th>
            <th>Price (cents)</th>
            <th>Stock Limit</th>
            <th>Sort</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          {% for row in variations %}
            <tr>
              <td>{{ row.name }}</td>
              <td class="mono">{{ row.price_cents }}</td>
              <td class="mono">{{ row.stock_limit ?? '' }}</td>
              <td class="mono">{{ row.sort_order }}</td>
              <td><wa-button href="/admin/events/{{ event.id }}/variations/{{ row.id }}" variant="neutral" size="s">Edit</wa-button></td>
            </tr>
          {% else %}
            <tr><td colspan="5">No variations yet.</td></tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
  </wa-card>
{% endblock %}
